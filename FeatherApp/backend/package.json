{
  "name": "feather-backend",
  "private": true,
  "version": "0.1.0",
  "description": "Helper scripts for running the Feather FastAPI backend via npm",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "black ."
  }
}

